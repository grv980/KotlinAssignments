import org.junit.jupiter.api.Test

import org.junit.jupiter.api.Assertions.*

internal class GetterandSettersTest {

    @Test
    fun main() {

        assertEquals("The client has 500 and has an interest rate of 1.0","The client has 500 and has an interest rate of 1.0")
        assertEquals("The client has 1000 and has an interest rate of 0.5","The client has 1000 and has an interest rate of 0.5")
        assertEquals("The client has 1200 and has an interest rate of 0.5","The client has 1200 and has an interest rate of 0.5")
    }
}