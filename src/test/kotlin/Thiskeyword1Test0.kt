import io.kotest.core.spec.style.FunSpec
import io.kotest.matchers.should
import io.kotest.matchers.shouldBe

class Thiskeyword1Test0 : FunSpec({

    test("main") {
        should { "Table height is 120 and size is 200" }
        should { "Table height is 150 and size is 250" }
    }
})
