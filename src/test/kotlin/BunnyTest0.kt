import io.kotest.core.spec.style.FunSpec
import io.kotest.matchers.should
import io.kotest.matchers.shouldBe

class BunnyTest0 : FunSpec({

    test("main") {
     should { "(\\(\\" }
     should { "(-.-)" }
     should { "o_(\")(\")" }
    }
})
