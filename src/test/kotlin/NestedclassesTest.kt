import org.junit.jupiter.api.Test

import org.junit.jupiter.api.Assertions.*

internal class NestedclassesTest {

    @Test
    fun main() {

        assertEquals("Getting ready for take off","Getting ready for take off")
        assertEquals("Engine engine1 started","Engine engine1 started")
        assertEquals("Engine engine2 started","Engine engine2 started")
        assertEquals("Taking off","Taking off")
    }

}